import assert from'node:assert/strict';import{normaliseGmail}from'./gmail-intake.mjs';
const msg={id:'fictional-gmail-1',internal_date:'1789330000000',label_ids:['INBOX'],payload:{mime_type:'text/plain',headers:[{name:'To',value:'Feedback <feedback@turbulentground.com>'},{name:'Message-ID',value:'<synthetic-1@example.invalid>'}],body:{content:'The results are confusing.\n\nOn Monday someone wrote:\n> Your previous results'}}};
const a=normaliseGmail(msg);assert.equal(a.disposition,'Ready');assert.equal(a.source.text,'The results are confusing.');assert.equal(normaliseGmail({...msg,id:'fictional-copy-2'}).source.id,a.source.id);assert.equal(normaliseGmail({...msg,label_ids:['SENT']}).disposition,'Ignore');assert.equal(normaliseGmail({...msg,payload:{...msg.payload,headers:[{name:'To',value:'unrelated@example.invalid'}]}}).disposition,'Ignore');assert.equal(normaliseGmail({...msg,payload:{...msg.payload,parts:[{filename:'image.png',body:{attachment_id:'fake'}}]}}).disposition,'Needs review');assert.equal(normaliseGmail({...msg,payload:{...msg.payload,headers:[...msg.payload.headers,{name:'Auto-Submitted',value:'auto-replied'}]}}).disposition,'Ignore');assert.equal(normaliseGmail({...msg,payload:{...msg.payload,mime_type:'text/html'}}).disposition,'Needs review');console.log('Passed Gmail recipient scope, quoted-mail separation, duplicate-copy identity, loop/exclusion filtering and ambiguous MIME/attachment review. Synthetic only; actual mirrored delivery unverified.');
