# Research project agent bootstrap

This directory belongs to the invite-only Turbulent Ground research project,
not the wider website sprint programme.

Before acting:

1. Read `work-state.json` first.
2. Read `agent-workflow.md` in full.
3. Follow the on-demand research workflow without claiming or modifying the
   main-site sprint state.
4. Keep participant data out of the repository and agent transcripts.

For the OpenSpec pilot attached to Airtable feedback `FB-20260911-TEVOGK`,
read `openspec/README.md` and its change review packet. ACT authorises
specification work only. Do not begin journey-indicator design, prototyping or
implementation until Graham explicitly approves the current specification and
`node openspec/airtable-sync.mjs check` passes against the live ticket.
OpenSpec artifact readiness is not approval.
